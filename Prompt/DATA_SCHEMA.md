# Data Schema

Projekt nie wykorzystuje bazy danych.

## Struktura plików:

* `.tf` – konfiguracja Terraform
* `.yml` – konfiguracja GitHub Actions
* `.md` – dokumentacja

## Przepływ danych:

* input: pliki `.tf`
* processing: GitHub Actions
* output: status pipeline (success/fail)
